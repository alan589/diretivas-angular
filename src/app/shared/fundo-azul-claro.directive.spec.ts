import { FundoAzulClaroDirective } from './fundo-azul-claro.directive';

describe('FundoAzulClaroDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoAzulClaroDirective();
    expect(directive).toBeTruthy();
  });
});
